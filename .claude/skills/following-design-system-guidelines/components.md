# Standard Components

- Button / Toggle / Slider / Picker / TextField などは標準サイズと間隔を守る。
- 色の適用は `foregroundStyle` / `tint` を用い、可読性優先。
- 混雑/重なりを避け、ガラス層を過密にしない。
- スクロール背後にコンテンツが来る場合は ScrollEdge を活用してコントラスト確保。
